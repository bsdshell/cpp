#include<iostream>
#include<stdio.h>
#include<string.h>

using namespace std;
int main(int argc, char** argv)
{
    string str = "The cat is block";
    char* array = new char[str.length+1];
    strncpy(array, str.c_str(), sizeof(array));
    std::cout<<array<<std::endl;
    delete array;
}

